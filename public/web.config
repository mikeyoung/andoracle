<?xml version="1.0" encoding="UTF-8"?>
<!--
  Andoracle — IIS configuration for the static PWA at /andoracle/.
  The manifest needs an explicit MIME type, and the generated service worker
  must bypass the origin cache so browser update checks receive fresh bytes.
-->
<configuration>
  <system.webServer>
    <defaultDocument>
      <files>
        <clear />
        <add value="index.html" />
      </files>
    </defaultDocument>
    <staticContent>
      <remove fileExtension=".webmanifest" />
      <mimeMap fileExtension=".webmanifest" mimeType="application/manifest+json" />
    </staticContent>
  </system.webServer>
  <location path="sw.js">
    <system.webServer>
      <staticContent>
        <clientCache cacheControlMode="DisableCache" />
      </staticContent>
      <!-- Some hosts apply a site-wide max-age after static-content rules.
           Explicit response headers keep service-worker update checks fresh
           through those hosting layers as well as through IIS itself. -->
      <httpProtocol>
        <customHeaders>
          <remove name="Cache-Control" />
          <add name="Cache-Control" value="no-cache, no-store, must-revalidate" />
          <remove name="Pragma" />
          <add name="Pragma" value="no-cache" />
          <remove name="Expires" />
          <add name="Expires" value="0" />
        </customHeaders>
      </httpProtocol>
    </system.webServer>
  </location>
</configuration>
